#!/bin/sh
set -eu
root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
cd "$root"
if [ "$#" -eq 0 ]; then
    set -- mirror
fi
if [ "${1:-}" = setup ]; then
    shift
    exec "$root/scripts/setup" "$@"
fi
exec "$root/.venv/bin/python" -m iphonebridge "$@"
